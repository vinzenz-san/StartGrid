# Abhängigkeiten
node_modules/

# Build-Ergebnis
dist/

# System-Dateien
.DS_Store
*.log
.vscode/
.idea/

# Extension builds / zip files
*.zip
dist.zip

# Environment variables (API keys) — never commit, .env.example is the template
.env
.env.local